# Release notes

What a user has to *act* on, and nothing else: a breaking change, a
migration, a default that moved under them. Everything a reader would
merely notice is in [CHANGELOG.md][record], which is the record behind
this file.

[record]: https://github.com/btclib-org/btclib-node/blob/main/CHANGELOG.md

## Unreleased

Nothing to act on. Nothing here is published to an index and what
anybody runs is a checkout of `main`, so there is no version to move
between; `RELEASING.md` says what that will look like when there is one.
